version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: mini-ecom-postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - mini_ecom_pgdata:/var/lib/postgresql/data

volumes:
  mini_ecom_pgdata:
